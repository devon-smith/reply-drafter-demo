# Copy to .env.local (gitignored) and fill in. Both are safe for the browser —
# the anon/publishable key is protected by Row-Level Security.
VITE_SUPABASE_URL=https://vpzzizeputfephcdbdpa.supabase.co
VITE_SUPABASE_ANON_KEY=REPLACE_WITH_PUBLISHABLE_OR_ANON_KEY
